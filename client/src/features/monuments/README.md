# Monuments Catalog Feature Boundary

## Architecture Overview
This directory houses components, search/filtering hooks, and presentation elements for the heritage monument catalog (`/explore`).
Monument models are consumed via the typed data layer (`@/data/monuments.ts`) and `@/types/monument.ts`.
